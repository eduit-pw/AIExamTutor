"""Resources package marker (PDFs, icons, licensing notice).

PDFs live in `resources/exam_sheets/`; see LICENSE_NOTICE.md inside it.
This module exists so `setuptools` treats `exam_sheets` as a package and
includes the data files listed in pyproject.toml [tool.setuptools.package-data].
"""
